import type {EnrichmentSource} from "../types/enrichment.ts";

export const SOURCE_UI: Record<EnrichmentSource, string> = {
    FINN: "Finn.no",
    NAV: "Arbeidsplassen: Nav",
    UNKNOWN: "Unknown"
};

